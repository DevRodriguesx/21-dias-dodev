let valor = Number(prompt('Digite um valor no qual seja um número inteiro e positivo '))

for (let contador = 0; contador <= valor ; contador++) {
    console.log('A contagem até seu número é: ' + contador);

}